<div class="container">
  <h1> <%= @event.name %></h1>
  <p><%= @event.total_price %></p>
  <% @event.participants.each do |participant|%>
    <p> <%= participant.first_name %> earns <%= participant.salary %></p>
    <% end %>
</div>
